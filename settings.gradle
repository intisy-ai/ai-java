rootProject.name = artifact_name
include ":shared"
include ":jvm"
include ":js"
// :providers is scaffolded in a later plan (its dir doesn't exist yet).
// Add it here once its module dir exists, e.g.:
// include ":providers"
// project(":providers").projectDir = file("providers")
