description=Server-side AI library for Java — providers, accounts/quota, and a routing proxy, ported from the intisy TypeScript AI stack.
example=```|AccountManager mgr = new AccountManager("claude-code", opts);|Acquired a = mgr.acquire("messages");|```
